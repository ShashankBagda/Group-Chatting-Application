// To convert UI file to PY file
python -m PyQt5.uic.pyuic -x main.ui -o main.py

// First make a [free PubNub account]
//(https://dashboard.pubnub.com/signup?devrel_gh=python-desktop-chat-application) to instantly get API keys.


// Command to Run --- Copy and Paste the follwing commands in CMD
// No need to run this commands now
// Just open the "configuration" file when you are running the application for the first time
// After that you have to only click the "run" file always.

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

python get-pip.py

dir

pip help

python -m pip install --upgrade pip

pip install -r requirements.txt

python MainWindow.py



